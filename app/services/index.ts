/**
 * Camada de serviços do LogiTrack (protótipo).
 * Sem backend: os dados são gerenciados pelo store Zustand (store/useLogisticaStore).
 */
export { useLogisticaStore } from "../store/useLogisticaStore";
